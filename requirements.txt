# SpectraTwin dependencies
#
# IMPORTANT: use Python 3.10 (recommended). On Python 3.13 the only available
# TensorFlow wheels (2.20/2.21) force protobuf>=6.31, which historically
# clashed with older Streamlit pins and caused hard-to-debug conflicts.
#
# macOS (Apple Silicon): xgboost needs the OpenMP runtime. If you see
# "libxgboost.dylib could not be loaded ... libomp.dylib", install it with:
#     conda install -c conda-forge llvm-openmp
#
# The versions below are a known-good, mutually compatible set (Python 3.10).

streamlit==1.58.0
pandas==2.3.3
numpy==2.2.6
scikit-learn==1.7.2
scipy==1.15.3
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.0.0
groq>=0.4.0
pillow>=9.0.0
requests>=2.28.0
altair>=5.0.0
tensorflow==2.21.0
protobuf>=6.31,<8
pywavelets==1.8.0
xgboost==3.2.0
optuna==4.9.0
bayesian-optimization>=1.4.0
openpyxl>=3.1.0
websockets>=11.0.0
watchdog>=3.0.0
# tkinter is included in Python standard library (see packages.txt for system dependencies)
